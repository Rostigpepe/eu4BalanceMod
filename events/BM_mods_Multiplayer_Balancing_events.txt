namespace = BM_MP
country_event = {
	id = BM_MP_war_events.1 # Defensive or Offensive war?
	title = BM_MP_war_events.1.t # We are at war with a stronger enemy, will we behave defensively or nah?
	desc = BM_MP_war_events.1.d
	picture = GENERALS_DISCUSSING_eventPicture
	
	trigger = {
		NOT = {
			has_country_flag = BM_MP_war_events_1_flag
		}
		is_at_war = yes
		any_war_enemy_country = { 
			ai = no
		}
		all_war_enemy_countries = { 
			army_strength = {
				who = ROOT
				value = 1.5
			}
		}
	}
	mean_time_to_happen = {
		months = 3
	}
	
	immediate = {
		set_country_flag = BM_MP_war_events_1_flag
	}
	
	option = { # The best defense is offense
		name = BM_MP_war_events.1.OPT1

	}

	option = { # Conserve our troops and play defensively
		name = BM_MP_war_events.1.OPT2
	}
}