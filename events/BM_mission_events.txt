namespace = BM_events_


country_event = {
	id = BM_events_FKN_conquest_event.1
	title = BM_events_FKN_conquest_event.1.t
	desc = BM_events_FKN_conquest_event.1.d
	picture = CONQUEST_eventPicture

	fire_only_once = yes
	is_triggered_only = yes

	option = {
		name = BM_events_FKN_conquest_event.1.a
		change_tag = FKN
		on_change_tag_effect = yes
		restore_country_name_effect = yes
		add_country_modifier = {
			name = "centralization_modifier"
			duration = 7300
		}
		add_prestige = 25
		set_country_flag = formed_franconia_flag

		if = {
			limit = {
				total_development = 100
				OR = {
					is_elector = yes
					is_part_of_hre = no
				}
			}
			set_government_rank = 2
		}

		swap_non_generic_missions = yes
		
		if = {
			limit = {
				is_playing_custom_nation = no
			}
			country_event = { id = ideagroups.1 } #Swap Ideas
		}
	}
}
